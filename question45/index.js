const vehical_info = (manufacturer, model, color) => {
  const myVehical = {
    manufacturer: manufacturer,
    model: model,
    color: color,
  };
  return myVehical;
};
console.log(vehical_info("honda", "2022", "red"));
