const username = ["admin", "user a", "user b", "user c", "user d"];

username.pop();
username.pop();
username.pop();
username.pop();
username.pop();

if (!username.length) console.log("We need to find some users!");
